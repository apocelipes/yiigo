package code

var (
	ErrParams   = New(10000, "params error")
	ErrAuth     = New(20000, "unauthenticated")
	ErrPerm     = New(30000, "permission denied")
	ErrNotFound = New(40000, "entity not found")
	ErrSystem   = New(50000, "internal server error")
	ErrService  = New(60000, "service error")
)
